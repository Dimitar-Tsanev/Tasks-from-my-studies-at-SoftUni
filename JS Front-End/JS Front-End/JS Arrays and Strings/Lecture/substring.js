function extractSubstring(string, index, numberOfCharacters){
    console.log(string.substring(index, index + numberOfCharacters));
}
extractSubstring('ASentence', 1, 8);
extractSubstring('SkipWord', 4, 7);